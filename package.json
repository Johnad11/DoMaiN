{
  "name": "do-main-it",
  "version": "1.0.0",
  "private": true,
  "description": "DO-MAIN-IT — Real-time multiplayer quiz platform with AI Quiz Forge and Domain Battles hex conquest",
  "main": "server/index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "postinstall": "npm install --prefix client",
    "build": "npm run build --prefix client || echo 'Using pre-built client'",
    "dev:server": "node server/index.js",
    "dev:client": "npm run dev --prefix client",
    "dev": "npm run build --prefix client && node server/index.js",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  }
}
